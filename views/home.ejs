<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Notícias - Documentação Inicial</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body class="bg-light">

    <div class="container py-5">
        <div class="text-center mb-4">
            <h1 class="fw-bold">API de Notícias</h1>
            <p class="text-muted">Esta API permite buscar, inserir, atualizar e deletar notícias utilizando o banco de
                dados MongoDB Atlas.</p>
        </div>

        <div class="card shadow-sm mb-4">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">Endpoints Disponíveis</h4>
            </div>
            <div class="card-body">

                <h5 class="mt-3">🔍 Buscar Notícias</h5>
                <ul>
                    <li><code>GET /noticias</code> — Retorna todas as notícias</li>
                    <li><code>GET /noticias/id/:id</code> — Retorna uma notícia pelo ID</li>
                    <li><code>GET /noticias/tiponoticia/:tipo</code> — Retorna notícias filtradas por tipo</li>
                </ul>

                <h5 class="mt-4">📝 Inserir Notícias</h5>
                <p><code>POST /postnoticias</code></p>
                <p>O corpo da requisição deve ser enviado em <strong>JSON</strong>, por exemplo:</p>

                <pre>{
  "titulo": "Título da notícia",
  "conteudo": "Conteúdo completo",
  "tipo": "esporte"
}</pre>

                <h5 class="mt-4">✏ Atualizar Notícias</h5>
                <p><code>PUT /putnoticias</code></p>
                <p>Enviar no corpo da requisição (JSON):</p>

                <pre>{
  "_id": "ID da notícia",
  "titulo": "Novo título",
  "conteudo": "Novo conteúdo",
  "tipo": "Novo tipo"
}</pre>

                <h5 class="mt-4">🗑 Deletar Notícias</h5>
                <p><code>DELETE /delnoticias</code></p>
                <p>Enviar no corpo da requisição (JSON):</p>

                <pre>{
  "_id": "ID da notícia"
}</pre>

            </div>
        </div>

        <footer class="text-center text-muted mt-4">
            <p>API desenvolvida com Node.js, Express e MongoDB Atlas.</p>
        </footer>
    </div>

</body>

</html>